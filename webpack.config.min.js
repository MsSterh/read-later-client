var webpack = require('webpack');
var config = require('./webpack.config');
var extend = require('extend');

var configMin = extend(true, {}, config);

configMin.plugins = configMin.plugins || [];
configMin.plugins.push(new webpack.optimize.UglifyJsPlugin({minimize: true}));

module.exports = configMin;
